{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "urn:robotics-runtime-contracts:evidence-index:v1",
  "title": "Robotics evidence index",
  "description": "Finalized index of local and confirmed remote evidence segments.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "schema_version",
    "run_id",
    "generated_at",
    "finalized",
    "segments"
  ],
  "properties": {
    "schema_version": {
      "const": "evidence-index.v1"
    },
    "run_id": {
      "$ref": "#/$defs/identifier"
    },
    "generated_at": {
      "type": "string",
      "format": "date-time"
    },
    "finalized": {
      "const": true
    },
    "segments": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/segment"
      }
    },
    "extensions": {
      "$ref": "#/$defs/extensions"
    }
  },
  "$defs": {
    "identifier": {
      "type": "string",
      "pattern": "^[a-z][a-z0-9]*(?:[._-][a-z0-9]+)*$",
      "maxLength": 128
    },
    "sha256": {
      "type": "string",
      "pattern": "^[a-f0-9]{64}$"
    },
    "segment": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "uri",
        "media_type",
        "sha256",
        "size_bytes",
        "retention_class",
        "segment_index",
        "upload_status",
        "checksum_verified"
      ],
      "properties": {
        "uri": {
          "type": "string",
          "pattern": "^(?:file|s3|oci|https)://[^\\s]+$"
        },
        "version_id": {
          "type": "string",
          "minLength": 1,
          "maxLength": 1024
        },
        "local_path": {
          "type": "string",
          "pattern": "^/[^*?]+$"
        },
        "media_type": {
          "enum": [
            "application/mcap",
            "application/json",
            "application/junit+xml",
            "text/plain",
            "application/vnd.in-toto+json"
          ]
        },
        "sha256": {
          "$ref": "#/$defs/sha256"
        },
        "size_bytes": {
          "type": "integer",
          "minimum": 0
        },
        "retention_class": {
          "type": "string",
          "pattern": "^[a-z][a-z0-9-]{1,63}$"
        },
        "segment_index": {
          "type": "integer",
          "minimum": 0
        },
        "upload_status": {
          "enum": [
            "local",
            "confirmed"
          ]
        },
        "checksum_verified": {
          "type": "boolean"
        }
      },
      "allOf": [
        {
          "if": {
            "required": [
              "uri"
            ],
            "properties": {
              "uri": {
                "pattern": "^s3://"
              }
            }
          },
          "then": {
            "required": [
              "version_id"
            ],
            "properties": {
              "upload_status": {
                "const": "confirmed"
              },
              "checksum_verified": {
                "const": true
              }
            }
          }
        },
        {
          "if": {
            "required": [
              "upload_status"
            ],
            "properties": {
              "upload_status": {
                "const": "local"
              }
            }
          },
          "then": {
            "required": [
              "local_path"
            ],
            "properties": {
              "uri": {
                "pattern": "^file://"
              },
              "checksum_verified": {
                "const": true
              }
            }
          }
        }
      ]
    },
    "extensions": {
      "type": "object",
      "default": {},
      "propertyNames": {
        "pattern": "^[a-z][a-z0-9-]*(?:\\.[a-z][a-z0-9-]*)+$"
      },
      "additionalProperties": {
        "type": "object"
      }
    }
  }
}
